name: react-jsx-dev
services:
  web:
    image: react-jsx-dev
    container_name: react-jsx-dev-web
    build: 
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
